(()=>{
  const esc=v=>String(v??'').replace(/[&<>\'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  async function api(payload){const r=await fetch('/api/early-access',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Unable to complete that request.');return d;}
  async function session(){const r=await fetch('/api/early-access',{cache:'no-store'}),d=await r.json().catch(()=>({loggedIn:false}));return r.ok?d:{loggedIn:false};}
  function markup(){return `<div class="early-access-shell glass-panel" id="earlyAccessShell">
    <div class="early-access-copy"><div class="eyebrow">EARLY ACCESS · HUBCORE VIBES</div><h3>Be part of the journey ahead.</h3><p>Apply for early access to HubCore Vibes. Your application is reviewed by the Founder. Once approved, you can sign in as an Early Access Member.</p><div class="early-steps"><span><b>1</b> Apply</span><span><b>2</b> Founder approval</span><span><b>3</b> Sign in</span></div></div>
    <div class="early-access-tabs" role="tablist"><button type="button" class="early-tab active" data-early-tab="apply">Apply for access</button><button type="button" class="early-tab" data-early-tab="login">Member sign in</button></div>
    <div class="early-pane active" data-early-pane="apply"><form id="earlyAccessApplyForm" class="early-form"><div class="field-row"><label>Full name<input name="name" autocomplete="name" maxlength="120" required></label><label>Email<input name="email" type="email" autocomplete="email" maxlength="200" required></label></div><div class="field-row"><label>Choose a username<input name="username" autocomplete="username" maxlength="40" placeholder="@yourname" required></label><label>Choose a password<input name="password" type="password" autocomplete="new-password" minlength="8" maxlength="128" placeholder="At least 8 characters" required></label></div><label>Why would you like early access?<textarea name="note" rows="4" maxlength="500" placeholder="Tell us what excites you about HubCore Vibes or what you would like to help shape."></textarea></label><button class="btn primary" type="submit">Apply for early access</button><div class="early-status" id="earlyApplyStatus" role="status" aria-live="polite"></div></form></div>
    <div class="early-pane" data-early-pane="login"><form id="earlyAccessLoginForm" class="early-form"><label>Email<input name="email" type="email" autocomplete="email" required></label><label>Password<input name="password" type="password" autocomplete="current-password" required></label><button class="btn primary" type="submit">Sign in to early access</button><div class="early-status" id="earlyLoginStatus" role="status" aria-live="polite"></div></form></div>
    <div class="early-member" id="earlyMember" hidden></div>
  </div>`;}
  function setTab(name){document.querySelectorAll('.early-tab').forEach(b=>b.classList.toggle('active',b.dataset.earlyTab===name));document.querySelectorAll('.early-pane').forEach(p=>p.classList.toggle('active',p.dataset.earlyPane===name));}
  function renderMember(profile){const box=document.getElementById('earlyMember');if(!box)return;box.hidden=false;document.querySelectorAll('.early-access-tabs,.early-pane').forEach(el=>el.hidden=true);box.innerHTML=`<div class="early-member-badge">✓ EARLY ACCESS MEMBER</div><h3>Welcome, ${esc(profile.name)}.</h3><p><strong>${esc(profile.username)}</strong> · Approved member</p><p>You are officially part of the HubCore Vibes early-access journey. You can take part in the live community and help shape what comes next.</p><div class="early-member-actions"><a class="btn primary" href="#community">Enter Community</a><a class="btn ghost" href="#reality">Explore Reality Switch</a><button class="btn ghost" id="earlyLogout" type="button">Sign out</button></div>`;document.getElementById('earlyLogout')?.addEventListener('click',async()=>{try{await api({action:'logout'});}finally{location.reload();}});}
  function install(){const section=document.getElementById('contact');if(!section||document.getElementById('earlyAccessShell'))return;section.insertAdjacentHTML('beforeend',markup());document.querySelectorAll('.early-tab').forEach(b=>b.addEventListener('click',()=>setTab(b.dataset.earlyTab)));
    document.getElementById('earlyAccessApplyForm')?.addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,status=document.getElementById('earlyApplyStatus'),button=form.querySelector('button[type="submit"]');if(!form.reportValidity())return;button.disabled=true;status.className='early-status';status.textContent='Sending your application…';try{const data=Object.fromEntries(new FormData(form).entries()),result=await api({action:'apply',...data});form.reset();status.className='early-status success';status.textContent=result.notificationSent?'Application received. The Founder has been emailed your approval request. You can sign in once it is approved.':'Application received and saved. Approval email delivery still needs to be configured by HubCore Vibes.';}catch(err){status.className='early-status error';status.textContent=err.message;}finally{button.disabled=false;}});
    document.getElementById('earlyAccessLoginForm')?.addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,status=document.getElementById('earlyLoginStatus'),button=form.querySelector('button[type="submit"]');if(!form.reportValidity())return;button.disabled=true;status.className='early-status';status.textContent='Signing in…';try{const data=Object.fromEntries(new FormData(form).entries()),result=await api({action:'login',...data});renderMember(result.profile);}catch(err){status.className='early-status error';status.textContent=err.message;}finally{button.disabled=false;}});
    session().then(s=>{if(s.loggedIn)renderMember(s.profile);}).catch(()=>{});
  }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();
