export async function onRequestGet({params,env}){if(!env?.MEDIA)return new Response('Media storage unavailable',{status:503});const path=Array.isArray(params.path)?params.path.join('/'):String(params.path||'');const object=await env.MEDIA.get(path);if(!object)return new Response('Not found',{status:404});const h=new Headers();object.writeHttpMetadata(h);h.set('etag',object.httpEtag);h.set('Cache-Control','public, max-age=31536000, immutable');return new Response(object.body,{headers:h})}