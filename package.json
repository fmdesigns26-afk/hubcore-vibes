{
  "name": "hubcore-vibes",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check script.js && node --check platform-api.js && node --check functions/lib/common.js && node --check functions/api/community/index.js && node --check functions/api/early-access.js && node --check functions/api/investor-contact.js",
    "dev": "npx wrangler pages dev .",
    "deploy": "npx wrangler pages deploy . --project-name=hubcore-vibes"
  }
}
